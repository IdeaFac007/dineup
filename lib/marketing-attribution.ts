import { createClient } from "./supabase/client";

const KEY="dineup_marketing_session";
const UTM_KEYS=["source","medium","campaign","content","term"] as const;
function readCookie(name:string){if(typeof document==="undefined")return"";const item=document.cookie.split(";").map(v=>v.trim()).find(v=>v.startsWith(`${name}=`));return item?decodeURIComponent(item.slice(name.length+1)):"";}
function sessionId(){if(typeof window==="undefined")return"";let id=localStorage.getItem(KEY);if(!id){id=`${crypto.randomUUID()}-${Date.now()}`;localStorage.setItem(KEY,id);}document.cookie=`${KEY}=${encodeURIComponent(id)}; path=/; max-age=31536000; SameSite=Lax`;return id;}
function params(){if(typeof window==="undefined")return{};const p=new URLSearchParams(window.location.search);return{source:p.get("utm_source")||readCookie("dineup_utm_source")||null,medium:p.get("utm_medium")||readCookie("dineup_utm_medium")||null,campaign:p.get("utm_campaign")||readCookie("dineup_utm_campaign")||null,content:p.get("utm_content")||readCookie("dineup_utm_content")||null,term:p.get("utm_term")||readCookie("dineup_utm_term")||null};}
export async function trackMarketingEvent(eventType:string,restaurantId?:number,metadata:Record<string,unknown>={}){if(typeof window==="undefined")return;try{const supabase=createClient();const u=new URL(window.location.href);const q=params();const sid=sessionId();for(const k of UTM_KEYS){const v=q[k];if(v)document.cookie=`dineup_utm_${k}=${encodeURIComponent(v)}; path=/; max-age=31536000; SameSite=Lax`;}await supabase.rpc("track_marketing_event",{p_session_id:sid,p_event_type:eventType,p_source:q.source,p_medium:q.medium,p_campaign:q.campaign,p_content:q.content,p_term:q.term,p_landing_path:u.pathname,p_referrer:document.referrer||null,p_restaurant_id:restaurantId||null,p_metadata:metadata});}catch(e){console.warn("Marketing attribution failed",e);}}
